```
Usage: ./auditCasAllUserCtrl.sh -s <sleep time> -o <output dir> -c <cas server> [ optional : -a <admin> ]
       ./auditCasAllUserCtrl.sh -s 10 -l "/tmp" -c default -a admin

        -s SLEEP_TIME
        -o LOG_PATH
        -c CAS_SERVER
        -a ADMIN
```

```
auditCasAllUserCtrl.sh -s 60 -o /tmp -c server02
```

```
Launch ./auditCasAllUserCtrl.sh with :
SLEEP_TIME : 60
LOG_PATH : /tmp
CAS_SERVER : server02
SAS_CLI_DEFAULT_CAS_SERVER : cas-shared-server02
MODE :
```
